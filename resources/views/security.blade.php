<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Vigilancia</title>
    <link rel="stylesheet" href="{{ asset('dashboard.css') }}">
</head>
<body>
    <div class="container">
        <header class="vigilancia">
            <h2>Bienvenido, {{ Auth::user()->name }}!</h2>
            <div>
                <a href="{{ url('/logout') }}" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                    Logout
                </a>
                <form id="logout-form" action="{{ url('/logout') }}" method="POST">
                    @csrf
                </form>
            </div>
        </header>
        <main>
            <h3>Lista de Invitados Aprobados</h3>
             Código para mostrar la lista de invitados aprobados 
            <h3>Registrar Entrada y Salida</h3>
            < Código para registrar la entrada y salida de los invitados 
        </main>
    </div>
</body>
</html> -->


<h1>Holiwi vigilancia</h1>